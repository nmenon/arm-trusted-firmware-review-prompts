Read the prompt {{REVIEW_DIR}}/review-core.md

If a Gerrit change ID or URL is provided, use it as the change to review.
If no change is specified, the protocol will ask for one.

Using the protocol, perform a full TF-A patch review of the specified Gerrit change.
